# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY  exclude tags voltDepCap.cir voltDepCap.cir.sh voltDepCap_baseline.cir voltDepCap_test.cir voltDepCharge_test.cir ageTempDep.cir DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
file(CHMOD ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap.cir.sh PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ WORLD_EXECUTE)
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir COMMAND ${XYCE_BINARY} ageTempDep.cir )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTIES FIXTURES_SETUP ageTempDep.cir)
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify COMMAND ${XYCE_VERIFY} ageTempDep.cir ${OutputDataDir}/SOLN_DEP_CAP/ageTempDep.cir.prn ageTempDep.cir.prn )
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify PROPERTIES FIXTURES_REQUIRED ageTempDep.cir)
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir COMMAND mpiexec -bind-to none -np 2 ${XYCE_BINARY} ageTempDep.cir )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTIES FIXTURES_SETUP ageTempDep.cir)
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify COMMAND ${XYCE_VERIFY} ageTempDep.cir ${OutputDataDir}/SOLN_DEP_CAP/ageTempDep.cir.prn ageTempDep.cir.prn )
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify PROPERTIES FIXTURES_REQUIRED ageTempDep.cir)
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
endif()
if( (NOT Xyce_PARALLEL_MPI) AND PERL_FOUND )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh COMMAND ${PERL_BIN} -I${XyceRegressionTestScripts} voltDepCap.cir.sh ${XYCE_BINARY} ${XYCE_VERIFY} ${XYCE_VERIFY} voltDepCap.cir ${OutputDataDir}/SOLN_DEP_CAP/voltDepCap.cir.prn )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh PROPERTY ATTACHED_FILES_ON_FAIL ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap.cir.err )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
endif()
if( Xyce_PARALLEL_MPI  AND PERL_FOUND )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh COMMAND ${PERL_BIN} -I${XyceRegressionTestScripts} voltDepCap.cir.sh "mpiexec -bind-to none -np 2 ${XYCE_BINARY}" ${XYCE_VERIFY} ${XYCE_VERIFY} voltDepCap.cir ${OutputDataDir}/SOLN_DEP_CAP/voltDepCap.cir.prn )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh PROPERTY ATTACHED_FILES_ON_FAIL ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap.cir.err )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
endif()
