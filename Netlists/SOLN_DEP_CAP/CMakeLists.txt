# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/exclude ${CMAKE_CURRENT_BINARY_DIR}/exclude ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/tags ${CMAKE_CURRENT_BINARY_DIR}/tags ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/voltDepCap.cir ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/voltDepCap.cir.sh ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap.cir.sh ONLY_IF_DIFFERENT)
file(CHMOD ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap.cir.sh PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ WORLD_EXECUTE)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/voltDepCap_baseline.cir ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap_baseline.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/voltDepCap_test.cir ${CMAKE_CURRENT_BINARY_DIR}/voltDepCap_test.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/voltDepCharge_test.cir ${CMAKE_CURRENT_BINARY_DIR}/voltDepCharge_test.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/ageTempDep.cir ${CMAKE_CURRENT_BINARY_DIR}/ageTempDep.cir ONLY_IF_DIFFERENT)
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir COMMAND $<TARGET_FILE:Xyce> ageTempDep.cir )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTIES FIXTURES_SETUP ageTempDep.cir)
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify COMMAND ${XYCE_VERIFY} ageTempDep.cir ${OutputDataDir}/SOLN_DEP_CAP/ageTempDep.cir.prn ageTempDep.cir.prn )
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify PROPERTIES FIXTURES_REQUIRED ageTempDep.cir)
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> ageTempDep.cir )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir PROPERTIES FIXTURES_SETUP ageTempDep.cir)
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify COMMAND ${XYCE_VERIFY} ageTempDep.cir ${OutputDataDir}/SOLN_DEP_CAP/ageTempDep.cir.prn ageTempDep.cir.prn )
  set_tests_properties(${TestNamePrefix}SOLN_DEP_CAP/ageTempDep.cir.verify PROPERTIES FIXTURES_REQUIRED ageTempDep.cir)
endif()
if( (NOT Xyce_PARALLEL_MPI) AND PERL_FOUND )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh COMMAND perl -I${XyceRegressionTestScripts} voltDepCap.cir.sh $<TARGET_FILE:Xyce> ${XYCE_VERIFY} ${XYCE_VERIFY} voltDepCap.cir ${OutputDataDir}/SOLN_DEP_CAP/voltDepCap.cir.prn )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
endif()
if( Xyce_PARALLEL_MPI  AND PERL_FOUND )
  add_test(NAME ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh COMMAND perl -I${XyceRegressionTestScripts} voltDepCap.cir.sh "mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce>" ${XYCE_VERIFY} ${XYCE_VERIFY} voltDepCap.cir ${OutputDataDir}/SOLN_DEP_CAP/voltDepCap.cir.prn )
  set_property(TEST ${TestNamePrefix}SOLN_DEP_CAP/voltDepCap.cir.sh PROPERTY LABELS "parallel;serial;nightly;expression;bsrc;subckt;voltdepcap;capacitor")
endif()
