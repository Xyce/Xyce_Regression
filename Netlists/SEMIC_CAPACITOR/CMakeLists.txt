# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/semicap.cir ${CMAKE_CURRENT_BINARY_DIR}/semicap.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/semicap_subc.cir ${CMAKE_CURRENT_BINARY_DIR}/semicap_subc.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/tags ${CMAKE_CURRENT_BINARY_DIR}/tags ONLY_IF_DIFFERENT)
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir COMMAND $<TARGET_FILE:Xyce> semicap.cir )
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir PROPERTIES FIXTURES_SETUP semicap.cir)
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir.verify COMMAND ${XYCE_VERIFY} semicap.cir ${OutputDataDir}/SEMIC_CAPACITOR/semicap.cir.prn semicap.cir.prn )
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir.verify PROPERTIES FIXTURES_REQUIRED semicap.cir)
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir.verify PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> semicap.cir )
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir PROPERTIES FIXTURES_SETUP semicap.cir)
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir.verify COMMAND ${XYCE_VERIFY} semicap.cir ${OutputDataDir}/SEMIC_CAPACITOR/semicap.cir.prn semicap.cir.prn )
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir.verify PROPERTIES FIXTURES_REQUIRED semicap.cir)
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap.cir.verify PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
endif()
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir COMMAND $<TARGET_FILE:Xyce> semicap_subc.cir )
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir PROPERTIES FIXTURES_SETUP semicap_subc.cir)
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir.verify COMMAND ${XYCE_VERIFY} semicap_subc.cir ${OutputDataDir}/SEMIC_CAPACITOR/semicap_subc.cir.prn semicap_subc.cir.prn )
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir.verify PROPERTIES FIXTURES_REQUIRED semicap_subc.cir)
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir.verify PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> semicap_subc.cir )
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir PROPERTIES FIXTURES_SETUP semicap_subc.cir)
  add_test(NAME ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir.verify COMMAND ${XYCE_VERIFY} semicap_subc.cir ${OutputDataDir}/SEMIC_CAPACITOR/semicap_subc.cir.prn semicap_subc.cir.prn )
  set_tests_properties(${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir.verify PROPERTIES FIXTURES_REQUIRED semicap_subc.cir)
  set_property(TEST ${TestNamePrefix}SEMIC_CAPACITOR/semicap_subc.cir.verify PROPERTY LABELS "serial;nightly;parallel;valgrind;semicap;capacitor")
endif()
