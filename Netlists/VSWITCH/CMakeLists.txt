# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files

file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/Vswitch_A1.cir ${CMAKE_CURRENT_BINARY_DIR}/Vswitch_A1.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/Vswitch_B1.cir ${CMAKE_CURRENT_BINARY_DIR}/Vswitch_B1.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/VswitchHyst1.cir ${CMAKE_CURRENT_BINARY_DIR}/VswitchHyst1.cir ONLY_IF_DIFFERENT)
file(COPY_FILE ${CMAKE_CURRENT_SOURCE_DIR}/tags ${CMAKE_CURRENT_BINARY_DIR}/tags ONLY_IF_DIFFERENT)
add_test(NAME VSWITCH/Vswitch_A1.cir COMMAND Xyce Vswitch_A1.cir )
set_property(TEST VSWITCH/Vswitch_A1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
set_tests_properties(VSWITCH/Vswitch_A1.cir PROPERTIES FIXTURES_SETUP Vswitch_A1.cir)
add_test(NAME VSWITCH/Vswitch_A1.cir.verify COMMAND ${XYCE_VERIFY} Vswitch_A1.cir ${OutputDataDir}/VSWITCH/Vswitch_A1.cir.prn Vswitch_A1.cir.prn )
set_tests_properties(VSWITCH/Vswitch_A1.cir.verify PROPERTIES FIXTURES_REQUIRED Vswitch_A1.cir)
add_test(NAME VSWITCH/VswitchHyst1.cir COMMAND Xyce VswitchHyst1.cir )
set_property(TEST VSWITCH/VswitchHyst1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
set_tests_properties(VSWITCH/VswitchHyst1.cir PROPERTIES FIXTURES_SETUP VswitchHyst1.cir)
add_test(NAME VSWITCH/VswitchHyst1.cir.verify COMMAND ${XYCE_VERIFY} VswitchHyst1.cir ${OutputDataDir}/VSWITCH/VswitchHyst1.cir.prn VswitchHyst1.cir.prn )
set_tests_properties(VSWITCH/VswitchHyst1.cir.verify PROPERTIES FIXTURES_REQUIRED VswitchHyst1.cir)
add_test(NAME VSWITCH/Vswitch_B1.cir COMMAND Xyce Vswitch_B1.cir )
set_property(TEST VSWITCH/Vswitch_B1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
set_tests_properties(VSWITCH/Vswitch_B1.cir PROPERTIES FIXTURES_SETUP Vswitch_B1.cir)
add_test(NAME VSWITCH/Vswitch_B1.cir.verify COMMAND ${XYCE_VERIFY} Vswitch_B1.cir ${OutputDataDir}/VSWITCH/Vswitch_B1.cir.prn Vswitch_B1.cir.prn )
set_tests_properties(VSWITCH/Vswitch_B1.cir.verify PROPERTIES FIXTURES_REQUIRED Vswitch_B1.cir)
