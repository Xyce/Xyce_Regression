# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY  Vswitch_A1.cir Vswitch_B1.cir VswitchHyst1.cir tags DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}VSWITCH/VswitchHyst1.cir COMMAND $<TARGET_FILE:Xyce> VswitchHyst1.cir )
  set_property(TEST ${TestNamePrefix}VSWITCH/VswitchHyst1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
  set_tests_properties(${TestNamePrefix}VSWITCH/VswitchHyst1.cir PROPERTIES FIXTURES_SETUP VswitchHyst1.cir)
  add_test(NAME ${TestNamePrefix}VSWITCH/VswitchHyst1.cir.verify COMMAND ${XYCE_VERIFY} VswitchHyst1.cir ${OutputDataDir}/VSWITCH/VswitchHyst1.cir.prn VswitchHyst1.cir.prn )
  set_tests_properties(${TestNamePrefix}VSWITCH/VswitchHyst1.cir.verify PROPERTIES FIXTURES_REQUIRED VswitchHyst1.cir)
  set_property(TEST ${TestNamePrefix}VSWITCH/VswitchHyst1.cir.verify PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}VSWITCH/VswitchHyst1.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> VswitchHyst1.cir )
  set_property(TEST ${TestNamePrefix}VSWITCH/VswitchHyst1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
  set_tests_properties(${TestNamePrefix}VSWITCH/VswitchHyst1.cir PROPERTIES FIXTURES_SETUP VswitchHyst1.cir)
  add_test(NAME ${TestNamePrefix}VSWITCH/VswitchHyst1.cir.verify COMMAND ${XYCE_VERIFY} VswitchHyst1.cir ${OutputDataDir}/VSWITCH/VswitchHyst1.cir.prn VswitchHyst1.cir.prn )
  set_tests_properties(${TestNamePrefix}VSWITCH/VswitchHyst1.cir.verify PROPERTIES FIXTURES_REQUIRED VswitchHyst1.cir)
  set_property(TEST ${TestNamePrefix}VSWITCH/VswitchHyst1.cir.verify PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
endif()
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_A1.cir COMMAND $<TARGET_FILE:Xyce> Vswitch_A1.cir )
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_A1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_A1.cir PROPERTIES FIXTURES_SETUP Vswitch_A1.cir)
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_A1.cir.verify COMMAND ${XYCE_VERIFY} Vswitch_A1.cir ${OutputDataDir}/VSWITCH/Vswitch_A1.cir.prn Vswitch_A1.cir.prn )
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_A1.cir.verify PROPERTIES FIXTURES_REQUIRED Vswitch_A1.cir)
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_A1.cir.verify PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_A1.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> Vswitch_A1.cir )
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_A1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_A1.cir PROPERTIES FIXTURES_SETUP Vswitch_A1.cir)
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_A1.cir.verify COMMAND ${XYCE_VERIFY} Vswitch_A1.cir ${OutputDataDir}/VSWITCH/Vswitch_A1.cir.prn Vswitch_A1.cir.prn )
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_A1.cir.verify PROPERTIES FIXTURES_REQUIRED Vswitch_A1.cir)
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_A1.cir.verify PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
endif()
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_B1.cir COMMAND $<TARGET_FILE:Xyce> Vswitch_B1.cir )
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_B1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_B1.cir PROPERTIES FIXTURES_SETUP Vswitch_B1.cir)
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_B1.cir.verify COMMAND ${XYCE_VERIFY} Vswitch_B1.cir ${OutputDataDir}/VSWITCH/Vswitch_B1.cir.prn Vswitch_B1.cir.prn )
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_B1.cir.verify PROPERTIES FIXTURES_REQUIRED Vswitch_B1.cir)
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_B1.cir.verify PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_B1.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> Vswitch_B1.cir )
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_B1.cir PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_B1.cir PROPERTIES FIXTURES_SETUP Vswitch_B1.cir)
  add_test(NAME ${TestNamePrefix}VSWITCH/Vswitch_B1.cir.verify COMMAND ${XYCE_VERIFY} Vswitch_B1.cir ${OutputDataDir}/VSWITCH/Vswitch_B1.cir.prn Vswitch_B1.cir.prn )
  set_tests_properties(${TestNamePrefix}VSWITCH/Vswitch_B1.cir.verify PROPERTIES FIXTURES_REQUIRED Vswitch_B1.cir)
  set_property(TEST ${TestNamePrefix}VSWITCH/Vswitch_B1.cir.verify PROPERTY LABELS "serial;nightly;parallel;switch;valgrind")
endif()
