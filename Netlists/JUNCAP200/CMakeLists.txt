# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY  DiodeClipper.cir tags DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}JUNCAP200/DiodeClipper.cir COMMAND ${XYCE_BINARY} DiodeClipper.cir )
  set_property(TEST ${TestNamePrefix}JUNCAP200/DiodeClipper.cir PROPERTY LABELS "serial;parallel;nightly;juncap200;adms")
  set_tests_properties(${TestNamePrefix}JUNCAP200/DiodeClipper.cir PROPERTIES FIXTURES_SETUP DiodeClipper.cir)
  add_test(NAME ${TestNamePrefix}JUNCAP200/DiodeClipper.cir.verify COMMAND ${XYCE_VERIFY} DiodeClipper.cir ${OutputDataDir}/JUNCAP200/DiodeClipper.cir.prn DiodeClipper.cir.prn )
  set_tests_properties(${TestNamePrefix}JUNCAP200/DiodeClipper.cir.verify PROPERTIES FIXTURES_REQUIRED DiodeClipper.cir)
  set_property(TEST ${TestNamePrefix}JUNCAP200/DiodeClipper.cir.verify PROPERTY LABELS "serial;parallel;nightly;juncap200;adms")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}JUNCAP200/DiodeClipper.cir COMMAND mpiexec -bind-to none -np 2 ${XYCE_BINARY} DiodeClipper.cir )
  set_property(TEST ${TestNamePrefix}JUNCAP200/DiodeClipper.cir PROPERTY LABELS "serial;parallel;nightly;juncap200;adms")
  set_tests_properties(${TestNamePrefix}JUNCAP200/DiodeClipper.cir PROPERTIES FIXTURES_SETUP DiodeClipper.cir)
  add_test(NAME ${TestNamePrefix}JUNCAP200/DiodeClipper.cir.verify COMMAND ${XYCE_VERIFY} DiodeClipper.cir ${OutputDataDir}/JUNCAP200/DiodeClipper.cir.prn DiodeClipper.cir.prn )
  set_tests_properties(${TestNamePrefix}JUNCAP200/DiodeClipper.cir.verify PROPERTIES FIXTURES_REQUIRED DiodeClipper.cir)
  set_property(TEST ${TestNamePrefix}JUNCAP200/DiodeClipper.cir.verify PROPERTY LABELS "serial;parallel;nightly;juncap200;adms")
endif()
