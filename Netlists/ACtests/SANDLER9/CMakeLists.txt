# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY  sandler9.cir sandler9.cir.sh tags DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
file(CHMOD ${CMAKE_CURRENT_BINARY_DIR}/sandler9.cir.sh PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ WORLD_EXECUTE)
if( (NOT Xyce_PARALLEL_MPI) AND PERL_FOUND )
  add_test(NAME ${TestNamePrefix}ACtests/SANDLER9/sandler9.cir.sh COMMAND perl -I${XyceRegressionTestScripts} sandler9.cir.sh ${XYCE_BINARY} ${XYCE_VERIFY} ${XYCE_VERIFY} sandler9.cir ${OutputDataDir}/ACtests/SANDLER9/sandler9.cir.prn )
  set_property(TEST ${TestNamePrefix}ACtests/SANDLER9/sandler9.cir.sh PROPERTY ATTACHED_FILES_ON_FAIL ${CMAKE_CURRENT_BINARY_DIR}/sandler9.cir.err )
  set_property(TEST ${TestNamePrefix}ACtests/SANDLER9/sandler9.cir.sh PROPERTY LABELS "serial;parallel;nightly;ac;resistor;subckt;capacitor;diode;bsrc;expression;undelimexpression;gsrc;isrc;hsrc;bjtlev1")
endif()
if( Xyce_PARALLEL_MPI  AND PERL_FOUND )
  add_test(NAME ${TestNamePrefix}ACtests/SANDLER9/sandler9.cir.sh COMMAND perl -I${XyceRegressionTestScripts} sandler9.cir.sh "mpiexec -bind-to none -np 2 ${XYCE_BINARY}" ${XYCE_VERIFY} ${XYCE_VERIFY} sandler9.cir ${OutputDataDir}/ACtests/SANDLER9/sandler9.cir.prn )
  set_property(TEST ${TestNamePrefix}ACtests/SANDLER9/sandler9.cir.sh PROPERTY ATTACHED_FILES_ON_FAIL ${CMAKE_CURRENT_BINARY_DIR}/sandler9.cir.err )
  set_property(TEST ${TestNamePrefix}ACtests/SANDLER9/sandler9.cir.sh PROPERTY LABELS "serial;parallel;nightly;ac;resistor;subckt;capacitor;diode;bsrc;expression;undelimexpression;gsrc;isrc;hsrc;bjtlev1")
endif()
