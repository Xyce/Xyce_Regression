# This file is generated by the script SetUpCtestFiles.py
# If possible, modify the script to fix any issues with the CMakeLists.txt files
# Or you can remove this header line to prevent this file from being overwritten

file(COPY  lcosc.cir tags DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
if( (NOT Xyce_PARALLEL_MPI) )
  add_test(NAME ${TestNamePrefix}WAMPDE/VCO/lcosc.cir COMMAND $<TARGET_FILE:Xyce> lcosc.cir )
  set_property(TEST ${TestNamePrefix}WAMPDE/VCO/lcosc.cir PROPERTY LABELS "serial;parallel;nightly;;wampde;mpde")
  set_tests_properties(${TestNamePrefix}WAMPDE/VCO/lcosc.cir PROPERTIES FIXTURES_SETUP lcosc.cir)
  add_test(NAME ${TestNamePrefix}WAMPDE/VCO/lcosc.cir.verify COMMAND ${XYCE_VERIFY} lcosc.cir ${OutputDataDir}/WAMPDE/VCO/lcosc.cir.prn lcosc.cir.prn )
  set_tests_properties(${TestNamePrefix}WAMPDE/VCO/lcosc.cir.verify PROPERTIES FIXTURES_REQUIRED lcosc.cir)
  set_property(TEST ${TestNamePrefix}WAMPDE/VCO/lcosc.cir.verify PROPERTY LABELS "serial;parallel;nightly;;wampde;mpde")
endif()
if( Xyce_PARALLEL_MPI  )
  add_test(NAME ${TestNamePrefix}WAMPDE/VCO/lcosc.cir COMMAND mpiexec -bind-to none -np 2 $<TARGET_FILE:Xyce> lcosc.cir )
  set_property(TEST ${TestNamePrefix}WAMPDE/VCO/lcosc.cir PROPERTY LABELS "serial;parallel;nightly;;wampde;mpde")
  set_tests_properties(${TestNamePrefix}WAMPDE/VCO/lcosc.cir PROPERTIES FIXTURES_SETUP lcosc.cir)
  add_test(NAME ${TestNamePrefix}WAMPDE/VCO/lcosc.cir.verify COMMAND ${XYCE_VERIFY} lcosc.cir ${OutputDataDir}/WAMPDE/VCO/lcosc.cir.prn lcosc.cir.prn )
  set_tests_properties(${TestNamePrefix}WAMPDE/VCO/lcosc.cir.verify PROPERTIES FIXTURES_REQUIRED lcosc.cir)
  set_property(TEST ${TestNamePrefix}WAMPDE/VCO/lcosc.cir.verify PROPERTY LABELS "serial;parallel;nightly;;wampde;mpde")
endif()
